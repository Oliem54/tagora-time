import "server-only";

/**
 * Lieu de ramassage / remise au client par defaut (sans saisie manuelle).
 * Aligné avec l’intitulé utilisé ailleurs dans l’app (ex. origine tournée « Oliem Solutions »).
 */
export const DEFAULT_RAMASSAGE_PICKUP_ADDRESS = "Oliem Solutions";
