import { redirect } from "next/navigation";

/** Ancienne vitrine marketing — redirigée vers l’accueil applicatif TAGORA Time. */
export default function EtiquettesRedirectPage() {
  redirect("/");
}
